import { BtnHighlightDirective } from './btn-highlight.directive';

describe('BtnHighlightDirective', () => {
  it('should create an instance', () => {
    const directive = new BtnHighlightDirective();
    expect(directive).toBeTruthy();
  });
});
